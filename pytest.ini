[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    real_catalog: isolated smoke checks that load the real Gold catalog and FAISS index

addopts =
    -v
    --tb=short
